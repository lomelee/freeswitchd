name: freeswitch master build docker image

# release tag 时触发执行
on:
 push

jobs:
  build:
    # 运行在那个虚拟机之上
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: check scr
      run: docker build . --file Dockerfile --tag icerleer/freeswitchd:$(date +%s)
